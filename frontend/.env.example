# Spotify API Configuration
# Get your Client ID from: https://developer.spotify.com/dashboard
REACT_APP_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Optional – Spotify redirect URI (production):
# Set this to your exact production URL so it matches Spotify Dashboard → Redirect URIs.
# Example: https://your-app.vercel.app (no trailing slash)
# REACT_APP_SPOTIFY_REDIRECT_URI=https://your-app.vercel.app
#
# Optional – Vercel dynamic URL (recommended on Vercel):
# In Vercel → Settings → Environment Variables, add:
#   Name: REACT_APP_VERCEL_URL   Value: $VERCEL_URL
# Then the redirect URI becomes https://<deployment-url> for each deployment.
# Add your production URL (and optionally a wildcard) in Spotify Redirect URIs.

# Google Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
REACT_APP_GEMINI_API_KEY=your_gemini_api_key_here
